# Prometheus — faz scrape (pull) das metricas expostas pelo OTel Collector.
# Intervalo curto para a demo; o unico alvo e o exporter do collector em :8889.

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
